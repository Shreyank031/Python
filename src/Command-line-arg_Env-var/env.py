import os  # Importing the os module to work with environment variables

# Printing the value of the "password" environment variable
print(os.getenv("password"))  # Retrieves the value of the "password" environment variable and prints it

